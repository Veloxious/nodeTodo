<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{pageName}}</title>
    <link rel="stylesheet" href="/css/master.css">
  </head>
  <body>
    <form class="" action="/" method="post">
      <div class="">
        <input type="text" name="todos" placeholder="Add a todo...">
        <input type="submit" class="buttons" name="submitBut" value="Add todo">
      </div>
      <div class="uncompleted_items">
        <h3>To Do</h3>
        {{#todos}}
          <button type="submit" name="done" class="buttons" value="{{.}}">Complete</button>{{.}}<br>
        {{/todos}}
      </div>
    </form>
      <div class="completed_items">
        <h3>Completed</h3>
        {{#done}}
          {{.}}<br>
        {{/done}}
      </div>
  </body>
</html>
